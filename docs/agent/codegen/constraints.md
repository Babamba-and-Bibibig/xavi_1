# Codegen Constraints

이 문서는 코드 생성 담당 AI 가 지켜야 하는 구현 제약을 적는다.

## 기본 규칙

- 클린 아키텍처 레이어를 침범하지 않는다.
- 안쪽 레이어는 바깥 레이어를 참조하지 않는다.
- 하네스는 별도 개발 시스템으로 유지한다.
- 파일은 책임 기준으로 배치한다.

## 작성 시작점

- 새 규칙이 생기면 이 문서에 먼저 추가한다.
- 구현 도중 반복되는 제한 조건도 이 문서에 누적한다.
